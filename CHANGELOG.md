## 2026-02-12
- 更新技能交互提示：默认输出“分支起点 + 变更摘要 + 功能候选清单”，禁止让用户按修订号选择范围；提示语统一为“分析服务/结果页”。
- 强化功能候选生成：清洗提交信息中的修订号噪音，提升功能描述可读性。
- 输出文案更自然：将“group-depth”等参数术语替换为中文描述。
- 增加 SVN/分析等待的心跳进度输出，并适度延长超时阈值，减少无返回感。
- 明确脚本固定位置，提示不要全盘搜索，只在 .codex/skills 范围内定位。
- 规范合并计划的对话输出：必须展示候选清单，避免“按编号/按路径”式硬性选项。
- 超时后要求先检查状态接口确认进度，避免直接判定失败。
- 补充“合并批注”定义与使用方式说明。
- 强制“每一次合并动作”都记录批注，避免只记录整功能总结。
- 列出合并改动时保留 SVN 修订号与提交信息，避免 AI 自编号。
- 明确在确认合并功能后，必须先深入代码理解改动，再进入合并步骤。
- 增加“合并前代码理解清单”，要求输出功能摘要与风险点后再合并。
- 调整插件 Activity Bar 图标为 currentColor 主题适配，避免在某些主题下不可见。
